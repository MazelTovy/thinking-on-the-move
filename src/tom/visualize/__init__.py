"""
Figure generation for the poster's signature plots:

- temporal_eval    — per-POI mass scatter, residual maps, density plots, NDCG curves
- scatter_4panel   — the in-sample × OOS, linear × log 4-panel chart
- visualize_map    — html basemap-backed maps + spatial hexbin diagnostics
"""
